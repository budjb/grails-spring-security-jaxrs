h3. Version 1.0.2
* Fix bug where Jax-RS security rules would only check the first rules in the list that matched the request HTTP \
  method, and failed when a different HTTP method was encountered.

h3. Version 1.0.1
* Fix broken SCM, documentation, and issues URLs.

h3. Version 1.0.0
* Initial public release.
* Refactor the Jax-RS filter invocation definition classes so they do not extend the base Grails Spring Security \
  version.

h3. Version 0.5.7
* Internal release.
